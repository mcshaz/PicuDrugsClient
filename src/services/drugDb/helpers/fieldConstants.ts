export const minVolume = 1.0;
export const maxVolume = 1000.0;
export const volErr = `Dilution Volume must be between ${minVolume} and ${maxVolume} mL`;
export const maxWeight = 100.0;
export const minWeight = 0.0;
export const wtErr = `Weight must be between ${minWeight} and ${maxWeight} Kg`;
export const maxAgeMth = 1200.0;
export const minAge = 0.0;
export const ageErr = `Age must be between ${minAge} and ${maxAgeMth / 12}yo (${maxAgeMth} months)`;
export const maxRate = 10000.0;
export const minRate = 0.0;
export const rateErr = `Rate must be between ${minRate} and ${maxRate}`;
